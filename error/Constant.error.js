/**
 * @overview Represents the error constants.
 */

// Name

const ANALYSIS_FAIL = 'Analysis fail'
const DOWNLOAD_FAIL = 'Download fail'
const CLEANING_FAIL = 'Cleaning fail'
const BAD_FORMAT = 'Bad format'
const BAD_OPERATION = 'Bad operation'

// Messages

const INPUT_INCORRECTLY_FORMATTED = 'Input incorrectly formatted'
const LOCATION_INCORRECTLY_FORMATTED = 'location incorrectly formatted'
const GIT_URL_FORMAT_INCORRECTLY_FORMATTED = 'Git URL format incorrectly formatted'
const ERROR_PARSING_JSON = 'Error while parsing JSON'
const MISSING_EMPTY_REPORT = 'Missing or empty static analysis result'

module.exports = {
  ANALYSIS_FAIL,
  DOWNLOAD_FAIL,
  CLEANING_FAIL,
  BAD_FORMAT,
  BAD_OPERATION,
  INPUT_INCORRECTLY_FORMATTED,
  LOCATION_INCORRECTLY_FORMATTED,
  GIT_URL_FORMAT_INCORRECTLY_FORMATTED,
  ERROR_PARSING_JSON,
  MISSING_EMPTY_REPORT
}
